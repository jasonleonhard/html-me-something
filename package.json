{
  "name": "html-me-something",
  "version": "1.0.0",
  "description": "html-me-something",
  "scripts": {
    "start": "npx -y harp . --port 9000",
    "build": "npx -y harp . build",
    "was": "harp server --port $PORT # build was harp compile # publish was www",
    "see": "open https://docs.netlify.com/integrations/frameworks/",
    "see2": "open https://harpjs.com/"
  },
  "dependencies": {
    "harp": "*"
  }
}